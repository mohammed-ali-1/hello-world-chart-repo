apiVersion: v1
kind: Service
metadata:
  name: {{ template "chart3.fullname" . }}
  labels:
    app: {{ template "chart3.name" . }}
    chart: {{ template "chart3.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "chart3.name" . }}
    release: {{ .Release.Name }}
